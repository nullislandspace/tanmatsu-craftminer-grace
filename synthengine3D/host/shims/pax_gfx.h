#pragma once
// Host stand-in for pax_gfx.h: only the names the engine's public scene
// headers mention. Nothing is drawn on a host build.
#include <stdint.h>
typedef struct pax_buf pax_buf_t;
typedef uint32_t       pax_col_t;
